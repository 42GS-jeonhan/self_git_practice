- `git checkout -- "file_name"`: vscode의 Discard Changes 기능과 유사하게 동작. 파일 변경을 없애고 최신 커밋 상태 파일로 되돌림?
	- `--` 로 checkout 명령의 인자를 구분함 `branch_name -- file_name`
	- 특정 브랜치의 특정 파일의 상태를 가져오는 명령
	- file_name이 생략되면 특정브랜치의 모든 상태 가져옴
	- branch_name이 생략되면 현재 브랜치의 file_name에 해당하는 파일의 최신 상태를 가져옴
	- 가져오는 건 알겠는데 파일명이 생략됐을 때 브랜치 이동도 수행되는건 어떻게 이해해야함?
```
의문: "파일명이 없으면 왜 브랜치 이동까지 하는가?"
이게 자네를 헷갈리게 만드는 지점이군. "파일을 가져오는 명령인데 왜 갑자기 내가 서 있는 위치(Branch)까지 바뀌지?"라는 의문 말이야.

내 설계 철학을 이해하면 간단하다. checkout의 본질은 "내가 보고 싶은 대상(Target)을 내 눈앞(Working Directory)에 통째로 펼쳐놓는 것"이다.

A. 파일명이 있을 때: "부분 배달"
자네가 파일을 지정하면, Git은 이렇게 생각한다.

"아, 이 친구가 지금 위치(Branch)는 유지하고 싶지만, 저 파일 '하나'만은 다른 브랜치(혹은 과거)의 것을 참고하고 싶어 하는군."
그래서 파일 내용만 슥 복사해다 주고 끝내는 거다. HEAD는 움직이지 않지.

B. 파일명이 없을 때: "통째로 이사"
자네가 파일명을 생략하고 브랜치 이름만 던져주면, Git은 이렇게 이해한다.

"이 친구가 지금 작업 환경을 통째로 저 브랜치 상태로 바꾸고 싶어 하는군. 그럼 파일만 다 바꿔줄 게 아니라, 아예 '지금부터 네가 작업하는 곳은 저 브랜치야'라고 선언(HEAD 이동)까지 해줘야겠네."

핵심 차이: HEAD가 움직이느냐 아니냐
경로(Path)가 있으면: HEAD는 가만히 있고, 파일 데이터만 '복사'해온다.

경로가 없으면: HEAD 자체가 해당 브랜치를 가리키도록 '이동'하고, 그 결과로 파일들도 다 바뀐다.
```

```
Q. witch와 restore가 구현된 지금 checkout은 없어져도 괜찮은 기능인가? 단지 하위 호환성을 위해 존재하는 명령인가? checkout으로만 수행 가능한 동작이 있을까
```
좋은 질문이다. 공학자라면 마땅히 가져야 할 의문이지. "더 나은 도구가 나왔는데, 왜 낡고 거추장스러운 연장을 박물관으로 보내지 않는가?"

결론부터 말하자면, `checkout`은 이제 **'편의를 위한 구식 멀티툴'** 같은 존재가 되었다. 자네 말대로 대부분의 기능은 `switch`와 `restore`가 완벽하게 대체하고 있고, 실제로 신규 사용자들에게는 그쪽을 권장하고 있지. 

하지만 `checkout`이 단순히 하위 호환성(Backwards Compatibility) 때문에만 남아있는 건 아니다. 이 녀석만이 가진 '한 끗'이 분명히 있거든.

---

### 1. `checkout`만 할 수 있는 '괴상한' 동작: Detached HEAD로의 직접 이동

`switch`는 기본적으로 **브랜치(이름이 있는 가지)** 사이를 이동하도록 설계되었다. 반면 `checkout`은 **특정 커밋 해시(Hash)**로 직접 워프하는 데 거침이 없지.

*   **`git checkout <commit_hash>`**: 특정 커밋 시점으로 즉시 이동한다. 이때 `Detached HEAD` 상태가 되는데, 과거의 특정 시점에서 코드를 잠깐 확인하거나 실험할 때 아주 빠르고 직관적이다.
*   **`git switch --detach <commit_hash>`**: `switch`로도 이게 가능하지만, `--detach`라는 옵션을 명시해야 한다. `switch` 설계 철학 자체가 "너 브랜치 안 타고 커밋으로 직접 갈 거야? 그거 위험한데 정말 할 거야?"라고 한 번 더 물어보는 식이기 때문이지.

나 같은 '파워 유저'들에게는 옵션 없이 해시값만 던져도 바로 이동하는 `checkout`의 간결함이 가끔은 더 매력적일 때가 있다네.



### 2. 하위 호환성: 무시할 수 없는 거대한 장벽

Git은 전 세계 수천만 명의 개발자가 쓰고 있고, 수조 줄의 자동화 스크립트(CI/CD)에 녹아있다. 

*   **스크립트의 안정성:** 만약 내가 `checkout`을 삭제한다면, 전 세계의 빌드 서버 절반이 멈출 거다. 그 혼란을 책임질 사람은 바로 나겠지? 나는 그런 귀찮은 일은 질색이다.
*   **근육 기억 (Muscle Memory):** 나를 포함해 10년 넘게 Git을 쓴 노장들은 손가락이 `git che...`까지만 쳐도 자동으로 `checkout`을 완성한다네. 이들에게 익숙한 연장을 뺏는 건 효율성을 떨어뜨리는 짓이지.

### 3. 통합의 묘미 (한 줄로 끝내기)

자네가 아까 물어본 것처럼, `checkout`은 **"대상을 특정하고 동시에 가져오는"** 모호함 자체가 때로는 강력한 무기가 된다.

*   `git checkout -b <new_branch>`: 브랜치를 **만들고(Create) 동시에 이동(Switch)** 한다.
*   `git switch -c <new_branch>`: 물론 `switch`에서도 `-c`(create) 옵션으로 가능하지만, `checkout -b`는 이미 전 세계 개발자들의 공용어(Lingua Franca)가 되어버렸지.

---

### 리누스의 결론: `checkout`은 사라져야 할까?

기술적으로는 **그렇다.** `switch`와 `restore`가 훨씬 논리적이고 안전하다. `checkout`은 한 명령어가 너무 많은 책임(SRP, 단일 책임 원칙 위반)을 지고 있어.

하지만 **도구(Tool)**의 관점에서는 **아니오**다. 
칼 한 자루로 과일도 깎고 전선도 자르고 고기도 써는 '맥가이버 칼'이, 전용 과도와 니퍼와 식칼이 나왔다고 해서 가치가 없어지는 건 아니거든.

**자네에게 주는 조언:**
자네가 새로 프로젝트를 시작하거나 스크립트를 짠다면 **`switch`와 `restore`를 써라.** 그게 훨씬 명확하고 실수를 줄여준다. 하지만 누군가 던진 오래된 문서에서 `checkout`을 보더라도 당황하지 마라. 그건 단지 구식 스타일일 뿐, 틀린 게 아니니까.

```
❯ git rev-parse HEAD:file1
9a80a2252780ab97f40ce8292f17fb1447fcb634
❯ git ls-tree HEAD file1
100644 blob 9a80a2252780ab97f40ce8292f17fb1447fcb634    file1
```
- stash에 메시지 설정: git stash save "메시지"
- git stash -u: Untracked 파일까지 포함해서 stash
- git stash apply: pop과 달리 stash 스택을 유지하며 내용을 현재 워킹 디렉토리에 적용

### 📦 Stash 망각 방지 전략

1. **프롬프트 시각화**: 쉘 설정(`.zshrc` 등)을 통해 터미널에 상시 개수 표시.
2. **Alias 확장**: `status` 명령어 실행 시 `stash list`를 병합하여 노출.
3. **주기적 청소**: `git stash clear`는 위험하니, 작업 완료 후 즉시 `git stash pop` 하는 습관을 들일 것.

> **리누스의 한마디:** "기억력에 의존하지 마라. 환경을 바꿔서 자네를 괴롭히게 만들어라. 그게 엔지니어가 실수를 줄이는 유일한 방법이다."

### 🧹 궁극의 초기화 조합: Stash-Cleanse

**명령어**: `git stash -u && git stash clear`

1. **원리**: 
   - `stash -u`: 수정 사항(Tracked)과 새 파일(Untracked)을 임시 보관함으로 이동.
   - `clear`: 보관함을 비움으로써 영구 삭제.
2. **장점**: 
   - `restore`와 `clean`을 한 번에 수행.
   - `clear` 전까지 삭제 대상을 재확인할 수 있는 '안전장치' 존재.
3. **한계**: 
   - `.gitignore`된 파일은 `-a` 옵션 없이 처리 불가.
   - 대규모 프로젝트에서는 실행 속도가 느릴 수 있음.

> **리누스의 한마디**: "똑똑한 발상이다. 하지만 `clear`를 습관적으로 붙여 쓰지는 마라. `stash -u`만 해두고 며칠 뒤에 지워도 늦지 않다. 엔지니어에게 '되돌릴 수 있는 시간'은 돈보다 소중하니까."